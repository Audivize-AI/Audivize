enum Utils {
    enum ML {}
    enum Images {}
}

enum Pairing {
    enum ASD {
        typealias Tracking = Pairing.Tracking
    }
    enum Tracking {
        typealias ASD = Pairing.ASD
    }
}

enum Voice {
    enum Diarization {}
    enum Transcription {}
}

